## Why

<!-- The problem this solves. Link an issue if there is one. -->

## What changed

<!-- The conceptual delta, not a file list — the diff shows the files. -->

## How it was tested

<!-- Commands and output. Unit tests, plus a real run if behavior changed (see system_tests/E2E.md). -->

---

- [ ] Tests added or updated; `pytest -m "not integration"` passes
- [ ] `ruff check src tests` is clean
- [ ] README/docs updated if behavior changed
